<div class="jumbotron">
	<%= render 'layouts/title' %>
		<div class="col-md-6 col-md-offset-3">
			<h3>Forgotten your password?</h3>
			<!-- <div class="panel panel-default"> -->
		 		 <!-- <div class="panel-heading"><h3 class="panel-title"><strong>Log in</strong></h3></div> -->
		  			<!-- <div class="panel-body"> -->
						<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), html: { class: 'form-horizontal'}) do |f| %>
				    	<%= f.input :email, :autofocus => true, label: "Email Address" %>
		<br />
	    <fieldset>
	   		<%= f.button :submit, "Reset Password", class: "btn btn-danger btn-large" %>
		</fieldset>
		<% end %>
		</div>
	</div>
</div>
</div>
</div>
<div class="col-md-8 col-md-offset-1" style="margin-top: 350px;">

	<%= render 'layouts/prefooter' %>

</div>

